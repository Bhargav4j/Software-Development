{
  "ConnectionStrings": {
    "DefaultConnection": "Host=${DB_HOST};Port=${DB_PORT};Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD};Pooling=true;Minimum Pool Size=${DB_MIN_POOL_SIZE};Maximum Pool Size=${DB_MAX_POOL_SIZE};Connection Lifetime=0;Command Timeout=${DB_COMMAND_TIMEOUT};Timeout=30"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  },
  "AllowedHosts": "${ALLOWED_HOSTS}"
}
